# ------------------------------------------------------------------------------
# <auto-generated>
#
#     This code was generated.
#
#     - To turn off auto-generation set:
#
#         [GitHubActions (AutoGenerate = false)]
#
#     - To trigger manual generation invoke:
#
#         nuke --generate-configuration GitHubActions_consume-new --host GitHubActions
#
# </auto-generated>
# ------------------------------------------------------------------------------

name: consume

on:
  push:
    branches:
      - master
    paths:
      - 'tips/_new/**/*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  ubuntu-latest:
    name: ubuntu-latest
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: 'Run: PostNewTip'
        run: ./build.cmd PostNewTip
        env:
          SlackWebhook: ${{ secrets.SLACK_WEBHOOK }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
